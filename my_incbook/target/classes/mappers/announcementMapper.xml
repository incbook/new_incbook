<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="my_incbook.mapper.announcementMapper">
	<resultMap id="AnnouncementVO" type="AnnouncementVO">
		<id property="id" column="id"/>
		<result property="regdate" column="regdate"/>
		<result property="title" column="title"/>
		<result property="content" column="content"/>
		<result property="memberId" column="member_id"/>
		<result property="viewCount" column="view_count"/>
	</resultMap>

	<select id="announcementList" resultMap="AnnouncementVO">
		<![CDATA[
		select 
		*
		from announcement
		]]>
	</select>

	<select id="readAnnouncement" resultMap="AnnouncementVO">
		<![CDATA[
		select 
		*
		from announcement where id = #{id}
		]]>
	</select>

	<update id="updateViewCnt">
		update announcement set view_count = view_count +1 where id = #{id}
	</update>
</mapper>